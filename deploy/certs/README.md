# Certificados (LAN)

Este directorio es para certificados **locales** (por ejemplo generados con `mkcert`) usados en despliegues LAN.

Archivos esperados (ejemplo):

- `cert.pem` (certificado)
- `key.pem` (clave privada)

> No commitear claves privadas. Ver `.gitignore`.

